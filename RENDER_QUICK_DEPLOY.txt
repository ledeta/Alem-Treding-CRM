================================================================================
                    RENDER DEPLOYMENT - QUICK REFERENCE
================================================================================

STATUS: READY TO DEPLOY ✅
COMMIT: ff37d7e4
DATE: August 19, 2026

================================================================================
DEPLOYMENT STEPS (5 MINUTES)
================================================================================

STEP 1: Verify Latest Code Pushed
   Status: ✅ DONE
   Command: git push origin main
   Current: ff37d7e4 (already pushed)

STEP 2: Go to Render Dashboard
   URL: https://dashboard.render.com
   Action: Sign in to your account

STEP 3: Create New Blueprint
   Click: "New +" → "Blueprint"
   Select: miliyee/Alem-Treding repository
   Action: Connect repository

STEP 4: Render Auto-Detects Configuration
   File: render.yaml ✅ (exists in root)
   Services detected:
      • alem-crm-frontend (Next.js)
      • alem-crm-backend (NestJS)
      • alem-crm-db (PostgreSQL)

STEP 5: Set Environment Variables
   JWT_SECRET: (Generate random string and enter)
   DATABASE_URL: (Auto-filled from PostgreSQL service)
   NODE_ENV: production (auto-filled)
   NEXT_PUBLIC_API_URL: https://alem-crm-backend.onrender.com (auto-filled)

STEP 6: Click "Create Blueprint"
   Render deploys all 3 services automatically
   Total time: ~15-20 minutes

STEP 7: Monitor Deployment
   Dashboard → Select service → Logs tab
   Watch for success messages

STEP 8: Verify Deployment
   Frontend: https://alem-crm-frontend.onrender.com
   Backend: https://alem-crm-backend.onrender.com/api/customers
   Test: Login and navigate through app

================================================================================
SERVICE URLS (AFTER DEPLOYMENT)
================================================================================

Frontend:   https://alem-crm-frontend.onrender.com
Backend:    https://alem-crm-backend.onrender.com
Database:   PostgreSQL (internal connection)

================================================================================
ENVIRONMENT VARIABLES NEEDED
================================================================================

FRONTEND (alem-crm-frontend):
   NODE_ENV = production
   NEXT_PUBLIC_API_URL = https://alem-crm-backend.onrender.com

BACKEND (alem-crm-backend):
   NODE_ENV = production
   DATABASE_URL = (auto-filled from PostgreSQL)
   JWT_SECRET = (you must set this - generate random string)
   PORT = 3001

DATABASE (alem-crm-db):
   Type: PostgreSQL 14
   Auto-configured by Render

================================================================================
DEPLOYMENT TIMELINE
================================================================================

PostgreSQL Start     ~1 min   🔄 Creating database
Backend Build        ~3 min   🔄 npm install & build
Backend Start        ~2 min   🔄 Starting NestJS
Frontend Build       ~4 min   🔄 npm install & build
Frontend Start       ~2 min   🔄 Starting Next.js

TOTAL: ~15-20 minutes from clicking "Create Blueprint"

================================================================================
WHAT TO VERIFY AFTER DEPLOYMENT
================================================================================

✅ Frontend loads without errors
   URL: https://alem-crm-frontend.onrender.com
   Expected: Login page visible

✅ Backend responds
   URL: https://alem-crm-backend.onrender.com/api/customers
   Expected: 200 status code, JSON response

✅ Database is connected
   Backend logs should show: "TypeOrmModule dependencies initialized"

✅ Login works
   Enter credentials → Should get JWT token

✅ Navigation works
   All 5 buttons clickable (Dashboard, Chat, Customers, Payments, Account)

✅ API calls work
   Customers page → Should load customer data from API

================================================================================
IF DEPLOYMENT FAILS
================================================================================

BUILD ERROR (Frontend):
   1. Check logs for error message
   2. Run locally: npm run build
   3. Fix TypeScript errors
   4. Push changes and redeploy

BUILD ERROR (Backend):
   1. Check logs for error message
   2. Check npm dependencies
   3. Verify environment variables set
   4. Push changes and redeploy

DATABASE ERROR:
   1. Check DATABASE_URL is set
   2. Check PostgreSQL service is running
   3. View database logs in Render

API NOT RESPONDING:
   1. Check backend URL in frontend env vars
   2. Check CORS configuration
   3. Check backend is serving on /api routes

================================================================================
AFTER DEPLOYMENT - WHAT HAPPENS AUTOMATICALLY
================================================================================

✅ Auto-redeploy on git push to main
✅ Auto-SSL certificates (HTTPS)
✅ Auto-backups of database
✅ Auto-scaling (if using paid plan)
✅ Auto-monitoring and alerts
✅ Auto-log storage (last 30 days)

================================================================================
SUPPORT LINKS
================================================================================

Render Dashboard:  https://dashboard.render.com
Render Docs:       https://render.com/docs
GitHub Repo:       https://github.com/miliyee/Alem-Treding
Logs:              render.com → Select service → Logs tab

================================================================================
IMPORTANT NOTES
================================================================================

1. JWT_SECRET must be set before deployment
   Generate random: Use OpenSSL, UUID generator, or any random string

2. DATABASE_URL is auto-filled from PostgreSQL service
   No need to set manually

3. NEXT_PUBLIC_API_URL must point to backend service URL
   Already configured in render.yaml

4. First deployment takes ~20 minutes
   Subsequent deployments (after code changes) are faster

5. Free tier limitations:
   • Services spin down after 15 minutes inactivity
   • PostgreSQL limited to 256MB
   • 0.5GB RAM per service
   
   Consider upgrading to Pro for production use

================================================================================
QUICK COMMANDS
================================================================================

Check current branch:
   git branch

Check if all changes are pushed:
   git status
   
View recent commits:
   git log --oneline -5

Push to GitHub (for Render to pick up):
   git push origin main

================================================================================

READY TO DEPLOY? ✅ YES

Next step: Go to https://dashboard.render.com and follow Step 1 above

================================================================================
